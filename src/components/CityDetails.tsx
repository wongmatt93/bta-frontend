import "./CityDetails.css";

// photo, rating, description, vote function, known for, extra information, don't do and food

const CityDetails = () => {
  return <div className="CityDetails">CityDetails works</div>;
};

export default CityDetails;
